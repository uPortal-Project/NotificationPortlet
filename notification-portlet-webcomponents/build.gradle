plugins {
    id "com.moowork.node" version "1.2.0"
}

node {
    version = nodejsVersion
    download = true
}

install.dependsOn('npm_run_build')
project.tasks.getByName('npm_run_bootstrap').dependsOn 'npm_ci'
project.tasks.getByName('npm_run_build').dependsOn 'npm_run_bootstrap'
